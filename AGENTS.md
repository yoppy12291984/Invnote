# 端末登録の申し送り

2026-09-24: index.htmlに端末登録を同梱。初回のみコード入力、資格はsuite-device-v1:Invnoteへ保存。通信失敗では削除せず、GASのdevice_revokedだけで再登録。

- GASは対応するOneDriveフォルダのCode.gsが統合ソース。保存済みデータの形式は維持。
- GAS所有者がcreateFirstDeviceCodeを一度実行して管理端末のコードを発行。以降はアプリの端末管理から招待を発行。復旧はrecoverManagerDeviceCode。
- 公開するのはindex.htmlと既存静的資産のみ。GASの控え・スクリプトプロパティ・登録コード・ユーザーデータをGitHubへ追加しない。
- 初回はホーム画面アプリを開いてコード登録。ブラウザー保存領域の消去・端末変更では再登録。期限ではログアウトしない。
- 同じgithub.ioの各アプリは同一オリジン。アプリ別の資格分離はあるが、相互のスクリプトからストレージを隔離する境界ではない。
- InvnoteのScreenerは公開データ閲覧の別画面。今回GASに接続するInvnote本体を対象。
- 各プロジェクトの運用・データ構造・計算処理を変更しない。


### 2026-09-24 端末表示を設定へ統合
画面に重なる「登録済み／端末管理」ボタンを廃止。設定内の「端末と共有」を開いて登録状態、招待コード発行、端末解除を操作する。Memoryは「接続設定」内。登録資格・保存形式・GASは変更なし。設定を再描画するLedgersでは同じ管理要素を再配置する。Invnote v3.13。
共通認証テストと5アプリの模擬通信ブラウザー確認済み。実iPhone/iPadの表示は未確認。


### 2026-09-24 曜日ToDo（v3.14）
ToDoタブに日付別リスト、追加・編集・削除、毎日/平日/指定曜日/特定日、メモ、日付別完了を追加。既存のタブ表示/起動設定に対応。S.todoRecordsで通常メモと分離し、GAS送信時だけnotesへkind=invnote-todo-v1の型付きレコードとして同梱。受信・JSON読込時はmergeInで分離し既存メモ数や投資計算に混ぜない。タスクと日付別チェックは別ID、編集時刻でマージ、削除は墓標。GASは既存のnotesマージで同期できるため変更なし。古い画面を開きっぱなしの端末では型付きToDoがメモとして見えるため全端末を再読込する。旧版へ戻す際もこのレコードを消さない。売買ルール/株式計算変更なし。tools/invnote-todos.test.cjsで曜日・日別完了・2クライアントの実GASマージ・削除保持・保存失敗復元を検証。ブラウザーで追加編集・再読込保持・翌日未完了・土曜除外を確認。本番端末間の同期実機確認は未実施。

